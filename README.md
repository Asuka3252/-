可直接导入时间分布、地区分布、人群分布等表格和地图文件(.json)，自动化生成传染病发病情况概述；
根据导入的数据，自动进行三间分布描述并作图（三线图、折线图和地图）；
文字概述可导出。
本地运行依赖Python环境，将代码另存为“app.py”,在桌面新建文件夹命名为“DiseaseAnalysis”，然后在cmd命令提示符窗口输入：cd Desktop\DiseaseAnalysis，按回车键，接下来输入：streamlit run app.py，按回车后，稍作等待网页会自动运行。
